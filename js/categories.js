/* ============================================================
   categories.js — Integrante A
   Navegacion por categorias.
   Requisito 11 del enunciado.
   ============================================================ */

export function initCategories() {
  const nav = document.getElementById('category-nav');
  console.log('categories listo', nav);
  // TODO(A)
}
